<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Work</title>
    <%- include('../layout/head.ejs') %>
</head>

<body>
    <header>
        <%- include('../layout/header.ejs') %>
    </header>
    <main>
        <%- include('../layout/main-load.ejs') %>
        <!-- the main data here -->
        <form action="/work" method="get">
            <input type="text" name="city" id="input-Work">
            <button id="work-button">Search</button>
        </form>
        <div id="flex">
            <% if(workinfo.length != 0){ 
               workinfo.forEach(item =>{ %>
            <div id="card">
                <h2> <%= item.title %></h2>
                <img src="<%= item.company_logo %>" width="100px" height="100px">
                <h4> <%= item.type %> </h4>
                <h4> <%= item.company %> </h4>
                <h4> <%= item.created_at %> </h4>
                <%- item.how_to_apply %>
                <button type="button" class="collapsible">Open Collapsible</button>
                <div class="content">
                    <p> <%- item.description %> </p>
                </div>
            </div>
            <% });
            } %>
        </div>
    </main>
    <footer>
        <%- include('../layout/footer.ejs') %>
    </footer>
</body>

</html>